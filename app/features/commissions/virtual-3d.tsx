import type { Route } from './+types/commissions';

export const meta: Route.MetaFunction = () => {
  return [{ title: 'Virtual 3D Commission' }];
};

export default function Virtual3D() {
  return null;
}
